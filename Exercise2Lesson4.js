/**reate a function that takes 1 parameter that is an object with 2 properties, name and age. Return true if the person's age is greater than 30 or their name c
 * ontains the letter x (case insensitive).
 *  Otherwise return false. */
Let people={ name: "Rex",age: 4};
let person={name:"dan",age:35};

     
          function function2(people1)   
             {
                 Let isOver30=people1.age>30;
                 let foundx=false;
                 for (let i = 0; i<people1.name.length;i++)
                  {
                     const element = array[people1];
                     
                 }
          if(people1.age>30||people1.name. =="x")
           {         
    return true;
          }else{
              return false;
          }
      }
console.log(function2(people));